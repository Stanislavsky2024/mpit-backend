generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model User {
  id        String   @id @default(uuid())
  email     String   @unique
  password  String
  createdAt DateTime @default(now()) @map("created_at")

  @@map("users")
}

model Category {
  id          String @id @default(uuid())
  name        String
  description String
  items       Item[] @relation("CategoryToItem")

  @@map("categories")
}

model Item {
  id              String    @id @default(uuid())
  categoryId      String?
  name            String
  imageUrl        String
  description     String
  price           Int
  characteristics Json
  category        Category? @relation("CategoryToItem", fields: [categoryId], references: [id], onDelete: Cascade)

  @@map("items")
}
